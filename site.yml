name: Monitor announcements and publish site

on:
  schedule:
    # 02:17, 08:17, 14:17, 20:17 China Standard Time; GitHub can delay scheduled runs.
    - cron: '17 0,6,12,18 * * *'
  workflow_dispatch:
  push:
    branches: [main]

concurrency:
  group: xuandiao-pages
  cancel-in-progress: false

permissions:
  contents: write
  pages: write
  id-token: write
  issues: write

jobs:
  monitor-and-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true
      - name: Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install scanner libraries
        run: pip install -r requirements.txt
      - name: Validate verified data and offline snapshot
        run: python build.py --check
      - name: Run offline regression tests
        run: python -m unittest discover -s tests
      - name: Check three-tier sources and 31 regional queries
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        run: python monitor.py
      - name: Save check results (never modify validated data.json)
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        run: |
          git config user.name 'xuandiao-monitor[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add watch_state.json review_queue.json third_sources.json monitor_status.json
          if ! git diff --cached --quiet; then
            git commit -m 'chore: update announcement monitoring status and review queue'
            git push origin HEAD:main
          fi
      - name: Open one GitHub issue for newly detected candidates
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          if [ "$(cat new_candidate_count.txt)" -gt 0 ]; then
            gh issue create --title "选调雷达：$(cat new_candidate_count.txt) 条新公告线索待核验" --body-file monitor_report.md || echo 'Issue creation unavailable: inspect review_queue.json manually.'
          fi
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Prepare public site
        run: |
          mkdir -p _site
          cp index.html data.json review_queue.json third_sources.json monitor_status.json _site/
          touch _site/.nojekyll
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@v4
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
